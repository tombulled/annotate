[tool.poetry]
name = "label"
version = "0.1.0"
description = "Label objects"
authors = ["Tom Bulled <26026015+tombulled@users.noreply.github.com>"]
license = "MIT"
readme = "README.md"
homepage = "https://github.com/tombulled/label"
repository = "https://github.com/tombulled/label"
keywords = ["python", "label", "annotate"]

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
